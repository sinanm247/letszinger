// ---------- Home Page Scripts ----------
// Note: Google Tag Manager is now added directly in index.html for better initialization
// GTM scripts should be in HTML head and body, not loaded dynamically via JavaScript
export const initHomePageScripts = () => {
    // Google Tag Manager is now in index.html
    // This function is kept for any future scripts that need to be initialized
    return () => {
        // Cleanup function (if needed in the future)
    };
};


// ---------- Thank You Page Scripts ----------
export const initThankYouPageScripts = () => {
};
